# Phase 2: Finance Infrastructure & Property Configuration — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the finance app shell (layout, navigation, middleware, auth helpers) and property configuration CRUD (properties, departments, fiscal devices, POS terminals) — the foundation all other finance features depend on.

**Architecture:** New `(finance)` route group with its own layout and sidebar. Server components for pages, client components for forms/tables. API routes follow existing pattern (`createClient()` → auth check → Supabase query → JSON response). Role-based access via `user_profiles` table.

**Tech Stack:** Next.js 16 App Router, Supabase (auth + RLS), Zod validation, shadcn/ui (table, card, dialog, select, badge, tabs, label, form), Tailwind CSS, TypeScript.

**Depends on:** Phase 1 (database foundation) — all tables, types, and schemas exist.

---

## File Structure

```
# Modified
middleware.ts                                        # Add /finance/* to protected routes

# New — Infrastructure
lib/finance/auth.ts                                  # getUserProfile(), requireRole(), role checks
app/(finance)/layout.tsx                             # Finance shell: sidebar + main content
app/(finance)/finance/page.tsx                       # Redirect to /finance/properties
components/finance/FinanceSidebar.tsx                 # Navigation sidebar (client component)

# New — Properties API
app/api/finance/properties/route.ts                  # GET (list), POST (create)
app/api/finance/properties/[id]/route.ts             # GET (single), PATCH (update)

# New — Properties UI
app/(finance)/finance/properties/page.tsx             # Property list (server component)
app/(finance)/finance/properties/new/page.tsx         # New property form page
app/(finance)/finance/properties/[id]/page.tsx        # Property detail with tabs
components/finance/PropertyList.tsx                   # Property table (client)
components/finance/PropertyForm.tsx                   # Property create/edit form (client)

# New — Departments API
app/api/finance/departments/route.ts                  # GET (list by property), POST (create)
app/api/finance/departments/[id]/route.ts             # PATCH (update)

# New — Departments UI
components/finance/DepartmentList.tsx                 # Department table within property detail
components/finance/DepartmentForm.tsx                 # Department create/edit dialog (client)

# New — Devices API
app/api/finance/fiscal-devices/route.ts               # GET (list by property), POST (create)
app/api/finance/fiscal-devices/[id]/route.ts          # DELETE
app/api/finance/pos-terminals/route.ts                # GET (list by property), POST (create)
app/api/finance/pos-terminals/[id]/route.ts           # DELETE

# New — Devices UI
components/finance/DeviceList.tsx                     # Fiscal devices table
components/finance/DeviceForm.tsx                     # Device create dialog
components/finance/TerminalList.tsx                   # POS terminals table
components/finance/TerminalForm.tsx                   # Terminal create dialog
```

---

### Task 1: Install additional shadcn/ui components

We need table, card, dialog, select, label, badge, tabs, dropdown-menu, and sheet components for the finance UI.

**Files:**
- Modified: `components/ui/` (new component files auto-generated by shadcn CLI)
- Modified: `package.json` (potential new deps)

- [ ] **Step 1: Install shadcn components**

```bash
npx shadcn@latest add table card dialog select label badge tabs dropdown-menu sheet form -y
```

- [ ] **Step 2: Verify installation**

```bash
ls components/ui/table.tsx components/ui/card.tsx components/ui/dialog.tsx components/ui/select.tsx components/ui/label.tsx components/ui/badge.tsx components/ui/tabs.tsx components/ui/dropdown-menu.tsx components/ui/sheet.tsx components/ui/form.tsx
```

Expected: all files exist.

- [ ] **Step 3: Verify build**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add components/ui/ package.json package-lock.json
git commit -m "feat: add shadcn/ui components for finance module"
```

---

### Task 2: Create finance auth helpers

Server-side helpers to get the current user's finance profile and enforce role-based access.

**Files:**
- Create: `lib/finance/auth.ts`

**Context:**
- The `user_profiles` table has `id` (FK to `auth.users`), `role`, `full_name`, `is_active`
- The `user_property_access` junction table maps users to properties
- Roles: `ADMIN_CO`, `FINANCE_CO`, `MANAGER`, `DEPT_HEAD`
- CO roles (`ADMIN_CO`, `FINANCE_CO`) can see all properties
- `MANAGER` and `DEPT_HEAD` can only see properties in `user_property_access`

- [ ] **Step 1: Create lib/finance/auth.ts**

```typescript
// lib/finance/auth.ts
import { createClient } from '@/lib/supabase/server'
import type { UserRole } from '@/types/finance'

export interface FinanceUser {
  id: string
  fullName: string
  role: UserRole
  isActive: boolean
}

/**
 * Get the current user's finance profile.
 * Returns null if not authenticated or no finance profile exists.
 */
export async function getFinanceUser(): Promise<FinanceUser | null> {
  const supabase = await createClient()
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return null

  const { data } = await supabase
    .from('user_profiles')
    .select('id, full_name, role, is_active')
    .eq('id', user.id)
    .single()

  if (!data || !data.is_active) return null

  return {
    id: data.id,
    fullName: data.full_name,
    role: data.role as UserRole,
    isActive: data.is_active,
  }
}

/**
 * Require the current user to have one of the specified roles.
 * Returns the FinanceUser if authorized, null otherwise.
 */
export async function requireRole(
  ...allowedRoles: UserRole[]
): Promise<FinanceUser | null> {
  const user = await getFinanceUser()
  if (!user) return null
  if (!allowedRoles.includes(user.role)) return null
  return user
}

/** CO roles can see everything; others are scoped to their properties */
export function isCORole(role: UserRole): boolean {
  return role === 'ADMIN_CO' || role === 'FINANCE_CO'
}
```

- [ ] **Step 2: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add lib/finance/auth.ts
git commit -m "feat: add finance auth helpers (getFinanceUser, requireRole)"
```

---

### Task 3: Extend middleware for finance routes

Add `/finance/*` to the list of protected routes in middleware.

**Files:**
- Modify: `middleware.ts`

**Context:**
- Current middleware protects `/chat/:path*` and handles auth page redirects
- We need to also protect `/finance/:path*`
- Same pattern: unauthenticated → redirect to `/login`
- After sign-in, keep default redirect to `/chat` (user navigates to finance via sidebar)

- [ ] **Step 1: Update middleware.ts**

Change the `isProtected` check and matcher config:

In `middleware.ts`, replace:

```typescript
  const isProtected = request.nextUrl.pathname.startsWith('/chat')
```

with:

```typescript
  const isProtected = request.nextUrl.pathname.startsWith('/chat') ||
                      request.nextUrl.pathname.startsWith('/finance')
```

And replace:

```typescript
export const config = {
  matcher: ['/chat/:path*', '/login', '/register'],
}
```

with:

```typescript
export const config = {
  matcher: ['/chat/:path*', '/finance/:path*', '/login', '/register'],
}
```

- [ ] **Step 2: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add middleware.ts
git commit -m "feat: extend middleware to protect /finance routes"
```

---

### Task 4: Create finance layout and sidebar

The finance module has its own layout with a sidebar for navigation between finance sections.

**Files:**
- Create: `components/finance/FinanceSidebar.tsx`
- Create: `app/(finance)/layout.tsx`
- Create: `app/(finance)/finance/page.tsx`

**Context:**
- Follow the same pattern as the chat sidebar (`components/sidebar/Sidebar.tsx`), but with navigation links instead of conversations
- The sidebar shows navigation grouped by role level:
  - Level 3 (DEPT_HEAD): Дневни отчети
  - Level 2 (MANAGER): Дневни отчети, Разходи, Тегления, Месечен отчет
  - Level 1 (CO): Всичко + Табло, Банки, Приходи, Кредити, Обекти (config)
- Use `usePathname()` to highlight active link
- The layout fetches `FinanceUser` on the server and passes to sidebar

- [ ] **Step 1: Create FinanceSidebar.tsx**

```tsx
// components/finance/FinanceSidebar.tsx
'use client'

import Link from 'next/link'
import { usePathname } from 'next/navigation'
import { cn } from '@/lib/utils'
import { ScrollArea } from '@/components/ui/scroll-area'
import { Separator } from '@/components/ui/separator'
import { Avatar, AvatarFallback } from '@/components/ui/avatar'
import { Badge } from '@/components/ui/badge'
import {
  Building2, LayoutDashboard, FileText, Receipt, Wallet,
  Landmark, ArrowRightLeft, TrendingUp, MessageSquare,
  CalendarDays, Settings,
} from 'lucide-react'
import type { UserRole } from '@/types/finance'

interface NavItem {
  href: string
  label: string
  icon: React.ComponentType<{ className?: string }>
  roles: UserRole[]
}

const navItems: NavItem[] = [
  { href: '/finance/dashboard', label: 'Табло', icon: LayoutDashboard, roles: ['ADMIN_CO', 'FINANCE_CO'] },
  { href: '/finance/daily-reports', label: 'Дневни отчети', icon: FileText, roles: ['ADMIN_CO', 'FINANCE_CO', 'MANAGER', 'DEPT_HEAD'] },
  { href: '/finance/expenses', label: 'Разходи', icon: Receipt, roles: ['ADMIN_CO', 'FINANCE_CO', 'MANAGER'] },
  { href: '/finance/withdrawals', label: 'Тегления', icon: Wallet, roles: ['ADMIN_CO', 'FINANCE_CO', 'MANAGER'] },
  { href: '/finance/cash-flow', label: 'Парични потоци', icon: ArrowRightLeft, roles: ['ADMIN_CO', 'FINANCE_CO'] },
  { href: '/finance/income', label: 'Приходи', icon: TrendingUp, roles: ['ADMIN_CO', 'FINANCE_CO'] },
  { href: '/finance/banking', label: 'Банки и кредити', icon: Landmark, roles: ['ADMIN_CO', 'FINANCE_CO'] },
  { href: '/finance/monthly', label: 'Месечен отчет', icon: CalendarDays, roles: ['ADMIN_CO', 'FINANCE_CO', 'MANAGER'] },
  { href: '/finance/properties', label: 'Обекти', icon: Building2, roles: ['ADMIN_CO'] },
]

interface Props {
  userFullName: string
  userRole: UserRole
}

export function FinanceSidebar({ userFullName, userRole }: Props) {
  const pathname = usePathname()

  const visibleItems = navItems.filter(item => item.roles.includes(userRole))

  const roleLabels: Record<UserRole, string> = {
    ADMIN_CO: 'Администратор',
    FINANCE_CO: 'Финанси ЦО',
    MANAGER: 'Управител',
    DEPT_HEAD: 'Началник отдел',
  }

  return (
    <div className="flex flex-col h-full w-64 bg-card border-r border-border">
      {/* Header */}
      <div className="p-4">
        <h1 className="text-sm font-semibold text-foreground mb-1">Финанси</h1>
        <p className="text-xs text-muted-foreground">Хотелска верига</p>
      </div>

      <Separator className="bg-border" />

      {/* Navigation */}
      <ScrollArea className="flex-1 p-2">
        <nav className="space-y-1">
          {visibleItems.map(item => {
            const Icon = item.icon
            const isActive = pathname.startsWith(item.href)
            return (
              <Link
                key={item.href}
                href={item.href}
                className={cn(
                  'flex items-center gap-3 px-3 py-2 rounded-md text-sm transition-colors',
                  isActive
                    ? 'bg-secondary text-foreground font-medium'
                    : 'text-muted-foreground hover:text-foreground hover:bg-secondary/50'
                )}
              >
                <Icon className="h-4 w-4 shrink-0" />
                {item.label}
              </Link>
            )
          })}
        </nav>
      </ScrollArea>

      <Separator className="bg-border" />

      {/* Chat link */}
      <div className="p-2">
        <Link
          href="/chat"
          className="flex items-center gap-3 px-3 py-2 rounded-md text-sm text-muted-foreground hover:text-foreground hover:bg-secondary/50 transition-colors"
        >
          <MessageSquare className="h-4 w-4 shrink-0" />
          AI Асистент
        </Link>
      </div>

      <Separator className="bg-border" />

      {/* User footer */}
      <div className="p-3 flex items-center gap-3">
        <Avatar className="h-7 w-7">
          <AvatarFallback className="bg-secondary text-muted-foreground text-xs">
            {userFullName[0]?.toUpperCase() ?? '?'}
          </AvatarFallback>
        </Avatar>
        <div className="flex-1 min-w-0">
          <p className="text-xs text-foreground truncate">{userFullName}</p>
          <Badge variant="outline" className="text-[10px] px-1 py-0">
            {roleLabels[userRole]}
          </Badge>
        </div>
      </div>
    </div>
  )
}
```

- [ ] **Step 2: Create finance layout**

```tsx
// app/(finance)/layout.tsx
import { redirect } from 'next/navigation'
import { getFinanceUser } from '@/lib/finance/auth'
import { FinanceSidebar } from '@/components/finance/FinanceSidebar'

export default async function FinanceLayout({
  children,
}: {
  children: React.ReactNode
}) {
  const user = await getFinanceUser()
  if (!user) redirect('/login')

  return (
    <div className="flex h-screen bg-background overflow-hidden">
      <FinanceSidebar
        userFullName={user.fullName}
        userRole={user.role}
      />
      <main className="flex-1 overflow-auto">
        {children}
      </main>
    </div>
  )
}
```

- [ ] **Step 3: Create finance index redirect**

```tsx
// app/(finance)/finance/page.tsx
import { redirect } from 'next/navigation'
import { getFinanceUser, isCORole } from '@/lib/finance/auth'

export default async function FinancePage() {
  const user = await getFinanceUser()
  if (!user) redirect('/login')

  // CO users see properties config (dashboard is a later phase), others see daily reports
  // NOTE: /finance/daily-reports page doesn't exist yet — will 404 until Phase 3
  if (isCORole(user.role)) {
    redirect('/finance/properties')
  }
  redirect('/finance/daily-reports')
}
```

- [ ] **Step 4: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add components/finance/FinanceSidebar.tsx app/(finance)/layout.tsx app/(finance)/finance/page.tsx
git commit -m "feat: add finance layout with role-based sidebar navigation"
```

---

### Task 5: Properties API — list and create

API routes for listing properties (scoped by role) and creating new ones (ADMIN_CO only).

**Files:**
- Create: `app/api/finance/properties/route.ts`

**Context:**
- Existing API pattern: `createClient()` → `supabase.auth.getUser()` → query → JSON response
- `createPropertySchema` from `lib/finance/schemas/property.ts` validates input
- CO roles see all properties; MANAGER/DEPT_HEAD see only their assigned properties via `user_property_access`
- Only `ADMIN_CO` can create properties
- Property fields: name, type, category, city, address, phone, email, eik, vat_number, mol, iban, bank, manager_id, authorized_person_id, active_since, status

- [ ] **Step 1: Create properties route.ts**

```typescript
// app/api/finance/properties/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser, isCORole } from '@/lib/finance/auth'
import { createPropertySchema } from '@/lib/finance/schemas'

export async function GET() {
  const user = await getFinanceUser()
  if (!user) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }

  const supabase = await createClient()

  let query = supabase
    .from('properties')
    .select('*')
    .order('name')

  // Non-CO users only see their assigned properties
  if (!isCORole(user.role)) {
    const { data: access } = await supabase
      .from('user_property_access')
      .select('property_id')
      .eq('user_id', user.id)

    const propertyIds = access?.map(a => a.property_id) ?? []
    if (propertyIds.length === 0) {
      return NextResponse.json([])
    }
    query = query.in('id', propertyIds)
  }

  const { data, error } = await query

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data)
}

export async function POST(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const body = await request.json()
  const parsed = createPropertySchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'validation_error', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const supabase = await createClient()

  const { data, error } = await supabase
    .from('properties')
    .insert({ ...parsed.data, created_by: user.id })
    .select()
    .single()

  if (error) {
    if (error.code === '23505') {
      return NextResponse.json(
        { error: 'duplicate', message: 'Обект с това име или ЕИК вече съществува' },
        { status: 409 }
      )
    }
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data, { status: 201 })
}
```

- [ ] **Step 2: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add app/api/finance/properties/route.ts
git commit -m "feat: add properties API (GET list, POST create)"
```

---

### Task 6: Properties API — get single and update

API route for getting a single property and updating it.

**Files:**
- Create: `app/api/finance/properties/[id]/route.ts`

**Context:**
- `updatePropertySchema` from `lib/finance/schemas/property.ts` validates partial updates
- Only `ADMIN_CO` can update properties
- Any authenticated finance user can GET a property (RLS handles row filtering)

- [ ] **Step 1: Create properties [id] route.ts**

```typescript
// app/api/finance/properties/[id]/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'
import { updatePropertySchema } from '@/lib/finance/schemas'

interface RouteParams {
  params: Promise<{ id: string }>
}

export async function GET(_request: NextRequest, { params }: RouteParams) {
  const { id } = await params
  const user = await getFinanceUser()
  if (!user) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('properties')
    .select('*')
    .eq('id', id)
    .single()

  if (error || !data) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 })
  }

  return NextResponse.json(data)
}

export async function PATCH(request: NextRequest, { params }: RouteParams) {
  const { id } = await params
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const body = await request.json()
  const parsed = updatePropertySchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'validation_error', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('properties')
    .update(parsed.data)
    .eq('id', id)
    .select()
    .single()

  if (error) {
    if (error.code === '23505') {
      return NextResponse.json(
        { error: 'duplicate', message: 'Обект с това име или ЕИК вече съществува' },
        { status: 409 }
      )
    }
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  if (!data) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 })
  }

  return NextResponse.json(data)
}
```

- [ ] **Step 2: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add app/api/finance/properties/[id]/route.ts
git commit -m "feat: add property detail API (GET single, PATCH update)"
```

---

### Task 7: Property list page

Server component that lists all properties in a table. Links to property detail and "new property" form.

**Files:**
- Create: `app/(finance)/finance/properties/page.tsx`
- Create: `components/finance/PropertyList.tsx`

**Context:**
- Server component fetches data, client component renders table
- Only `ADMIN_CO` sees the "Нов обект" button and can access this page
- Table columns: Име, Тип, Град, ЕИК, Управител, Статус
- Each row links to `/finance/properties/[id]`
- Follow existing pattern from `app/(app)/chat/[id]/page.tsx`

- [ ] **Step 1: Create PropertyList.tsx**

```tsx
// components/finance/PropertyList.tsx
'use client'

import Link from 'next/link'
import { Badge } from '@/components/ui/badge'
import {
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import type { Property } from '@/types/finance'

const typeLabels: Record<string, string> = {
  HOTEL: 'Хотел',
  APARTMENT_HOTEL: 'Апарт хотел',
  HOSTEL: 'Хостел',
  SHOP: 'Магазин',
  OTHER: 'Друг',
}

interface Props {
  properties: Property[]
}

export function PropertyList({ properties }: Props) {
  if (properties.length === 0) {
    return (
      <p className="text-sm text-muted-foreground py-8 text-center">
        Няма добавени обекти.
      </p>
    )
  }

  return (
    <Table>
      <TableHeader>
        <TableRow>
          <TableHead>Име</TableHead>
          <TableHead>Тип</TableHead>
          <TableHead>Град</TableHead>
          <TableHead>ЕИК</TableHead>
          <TableHead>Статус</TableHead>
        </TableRow>
      </TableHeader>
      <TableBody>
        {properties.map(property => (
          <TableRow key={property.id}>
            <TableCell>
              <Link
                href={`/finance/properties/${property.id}`}
                className="text-foreground hover:underline font-medium"
              >
                {property.name}
              </Link>
            </TableCell>
            <TableCell className="text-muted-foreground">
              {typeLabels[property.type] ?? property.type}
            </TableCell>
            <TableCell className="text-muted-foreground">{property.city}</TableCell>
            <TableCell className="text-muted-foreground font-mono text-xs">
              {property.eik}
            </TableCell>
            <TableCell>
              <Badge variant={property.status === 'ACTIVE' ? 'default' : 'secondary'}>
                {property.status === 'ACTIVE' ? 'Активен' : 'Неактивен'}
              </Badge>
            </TableCell>
          </TableRow>
        ))}
      </TableBody>
    </Table>
  )
}
```

- [ ] **Step 2: Create properties page.tsx**

```tsx
// app/(finance)/finance/properties/page.tsx
import Link from 'next/link'
import { redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { requireRole } from '@/lib/finance/auth'
import { PropertyList } from '@/components/finance/PropertyList'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import { Plus } from 'lucide-react'
import type { Property } from '@/types/finance'

export default async function PropertiesPage() {
  const user = await requireRole('ADMIN_CO')
  if (!user) redirect('/finance')

  const supabase = await createClient()
  const { data: properties } = await supabase
    .from('properties')
    .select('*')
    .order('name')

  return (
    <div className="p-6 max-w-6xl mx-auto">
      <Card>
        <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-4">
          <CardTitle className="text-lg">Обекти</CardTitle>
          <Button asChild size="sm">
            <Link href="/finance/properties/new">
              <Plus className="h-4 w-4 mr-2" />
              Нов обект
            </Link>
          </Button>
        </CardHeader>
        <CardContent>
          <PropertyList properties={(properties as Property[]) ?? []} />
        </CardContent>
      </Card>
    </div>
  )
}
```

- [ ] **Step 3: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add components/finance/PropertyList.tsx app/(finance)/finance/properties/page.tsx
git commit -m "feat: add property list page with table"
```

---

### Task 8: Property form (create and edit)

Reusable form component for creating and editing properties. Used on both the "new" page and the property detail page.

**Files:**
- Create: `components/finance/PropertyForm.tsx`
- Create: `app/(finance)/finance/properties/new/page.tsx`

**Context:**
- `createPropertySchema` validates all fields; `updatePropertySchema` is partial
- Property form fields (from schema): name, type, category, city, address, phone, email, eik, vat_number, mol, iban, bank, manager_id, authorized_person_id, active_since, status
- `manager_id` and `authorized_person_id` are UUIDs referencing `user_profiles` — for now, render as text inputs (user ID); full user picker is a later phase
- Form submits to `POST /api/finance/properties` (create) or `PATCH /api/finance/properties/[id]` (edit)
- After successful create, redirect to `/finance/properties/[id]`
- After successful edit, show success message and refresh data
- Bulgarian labels for all fields

- [ ] **Step 1: Create PropertyForm.tsx**

```tsx
// components/finance/PropertyForm.tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import {
  Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from '@/components/ui/select'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import type { Property } from '@/types/finance'

const propertyTypes = [
  { value: 'HOTEL', label: 'Хотел' },
  { value: 'APARTMENT_HOTEL', label: 'Апарт хотел' },
  { value: 'HOSTEL', label: 'Хостел' },
  { value: 'SHOP', label: 'Магазин' },
  { value: 'OTHER', label: 'Друг' },
]

const categoryOptions = [
  { value: '1_STAR', label: '1 звезда' },
  { value: '2_STAR', label: '2 звезди' },
  { value: '3_STAR', label: '3 звезди' },
  { value: '4_STAR', label: '4 звезди' },
  { value: '5_STAR', label: '5 звезди' },
  { value: 'NONE', label: 'Без категория' },
]

const statusOptions = [
  { value: 'ACTIVE', label: 'Активен' },
  { value: 'INACTIVE', label: 'Неактивен' },
]

interface Props {
  property?: Property
}

export function PropertyForm({ property }: Props) {
  const router = useRouter()
  const isEdit = !!property
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setError(null)
    setLoading(true)

    const formData = new FormData(e.currentTarget)
    const body: Record<string, unknown> = {}

    for (const [key, value] of formData.entries()) {
      if (typeof value === 'string' && value.trim() !== '') {
        body[key] = value.trim()
      }
    }

    try {
      const url = isEdit
        ? `/api/finance/properties/${property.id}`
        : '/api/finance/properties'
      const method = isEdit ? 'PATCH' : 'POST'

      const res = await fetch(url, {
        method,
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
      })

      if (!res.ok) {
        const data = await res.json()
        setError(data.message ?? data.error ?? 'Грешка при запис')
        return
      }

      const saved = await res.json()
      if (isEdit) {
        router.refresh()
      } else {
        router.push(`/finance/properties/${saved.id}`)
      }
    } catch {
      setError('Грешка при връзка със сървъра')
    } finally {
      setLoading(false)
    }
  }

  return (
    <Card>
      <CardHeader>
        <CardTitle className="text-lg">
          {isEdit ? 'Редактиране на обект' : 'Нов обект'}
        </CardTitle>
      </CardHeader>
      <CardContent>
        <form onSubmit={handleSubmit} className="space-y-4">
          {error && (
            <p className="text-sm text-destructive bg-destructive/10 px-3 py-2 rounded">
              {error}
            </p>
          )}

          <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
            {/* Name */}
            <div className="space-y-2">
              <Label htmlFor="name">Име *</Label>
              <Input id="name" name="name" required defaultValue={property?.name ?? ''} />
            </div>

            {/* Type */}
            <div className="space-y-2">
              <Label htmlFor="type">Тип *</Label>
              <Select name="type" required defaultValue={property?.type ?? ''}>
                <SelectTrigger><SelectValue placeholder="Избери тип" /></SelectTrigger>
                <SelectContent>
                  {propertyTypes.map(t => (
                    <SelectItem key={t.value} value={t.value}>{t.label}</SelectItem>
                  ))}
                </SelectContent>
              </Select>
            </div>

            {/* Category */}
            <div className="space-y-2">
              <Label htmlFor="category">Категория *</Label>
              <Select name="category" required defaultValue={property?.category ?? ''}>
                <SelectTrigger><SelectValue placeholder="Избери категория" /></SelectTrigger>
                <SelectContent>
                  {categoryOptions.map(c => (
                    <SelectItem key={c.value} value={c.value}>{c.label}</SelectItem>
                  ))}
                </SelectContent>
              </Select>
            </div>

            {/* City */}
            <div className="space-y-2">
              <Label htmlFor="city">Град *</Label>
              <Input id="city" name="city" required defaultValue={property?.city ?? ''} />
            </div>

            {/* Address */}
            <div className="space-y-2 md:col-span-2">
              <Label htmlFor="address">Адрес *</Label>
              <Input id="address" name="address" required defaultValue={property?.address ?? ''} />
            </div>

            {/* Phone */}
            <div className="space-y-2">
              <Label htmlFor="phone">Телефон</Label>
              <Input id="phone" name="phone" defaultValue={property?.phone ?? ''} />
            </div>

            {/* Email */}
            <div className="space-y-2">
              <Label htmlFor="email">Email</Label>
              <Input id="email" name="email" type="email" defaultValue={property?.email ?? ''} />
            </div>

            {/* EIK */}
            <div className="space-y-2">
              <Label htmlFor="eik">ЕИК *</Label>
              <Input id="eik" name="eik" required pattern="\d{9}" maxLength={9}
                defaultValue={property?.eik ?? ''} placeholder="123456789" />
            </div>

            {/* VAT Number */}
            <div className="space-y-2">
              <Label htmlFor="vat_number">ДДС номер</Label>
              <Input id="vat_number" name="vat_number" defaultValue={property?.vat_number ?? ''} />
            </div>

            {/* MOL */}
            <div className="space-y-2">
              <Label htmlFor="mol">МОЛ *</Label>
              <Input id="mol" name="mol" required defaultValue={property?.mol ?? ''} />
            </div>

            {/* IBAN */}
            <div className="space-y-2">
              <Label htmlFor="iban">IBAN</Label>
              <Input id="iban" name="iban" defaultValue={property?.iban ?? ''} />
            </div>

            {/* Bank */}
            <div className="space-y-2">
              <Label htmlFor="bank">Банка</Label>
              <Input id="bank" name="bank" defaultValue={property?.bank ?? ''} />
            </div>

            {/* Manager ID */}
            <div className="space-y-2">
              <Label htmlFor="manager_id">Управител (ID) *</Label>
              <Input id="manager_id" name="manager_id" required
                defaultValue={property?.manager_id ?? ''} placeholder="UUID на управител" />
            </div>

            {/* Authorized Person ID */}
            <div className="space-y-2">
              <Label htmlFor="authorized_person_id">Упълномощено лице (ID)</Label>
              <Input id="authorized_person_id" name="authorized_person_id"
                defaultValue={property?.authorized_person_id ?? ''} placeholder="UUID (незадължително)" />
            </div>

            {/* Active Since */}
            <div className="space-y-2">
              <Label htmlFor="active_since">Активен от *</Label>
              <Input id="active_since" name="active_since" type="date" required
                defaultValue={property?.active_since ?? ''} />
            </div>

            {/* Status (edit only) */}
            {isEdit && (
              <div className="space-y-2">
                <Label htmlFor="status">Статус</Label>
                <Select name="status" defaultValue={property?.status ?? 'ACTIVE'}>
                  <SelectTrigger><SelectValue /></SelectTrigger>
                  <SelectContent>
                    {statusOptions.map(s => (
                      <SelectItem key={s.value} value={s.value}>{s.label}</SelectItem>
                    ))}
                  </SelectContent>
                </Select>
              </div>
            )}
          </div>

          <div className="flex gap-3 pt-4">
            <Button type="submit" disabled={loading}>
              {loading ? 'Запис...' : isEdit ? 'Запази' : 'Създай обект'}
            </Button>
            <Button type="button" variant="outline" onClick={() => router.back()}>
              Отказ
            </Button>
          </div>
        </form>
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 2: Create new property page**

```tsx
// app/(finance)/finance/properties/new/page.tsx
import { redirect } from 'next/navigation'
import { requireRole } from '@/lib/finance/auth'
import { PropertyForm } from '@/components/finance/PropertyForm'

export default async function NewPropertyPage() {
  const user = await requireRole('ADMIN_CO')
  if (!user) redirect('/finance')

  return (
    <div className="p-6 max-w-3xl mx-auto">
      <PropertyForm />
    </div>
  )
}
```

- [ ] **Step 3: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add components/finance/PropertyForm.tsx app/(finance)/finance/properties/new/page.tsx
git commit -m "feat: add property create/edit form and new property page"
```

---

### Task 9: Property detail page with tabs

Property detail page showing property info, departments, fiscal devices, and POS terminals in tabs.

**Files:**
- Create: `app/(finance)/finance/properties/[id]/page.tsx`

**Context:**
- Server component that fetches property + related data
- Uses shadcn Tabs with 4 tabs: Информация (edit form), Отдели, Фискални устройства, ПОС терминали
- Passes data to client components (PropertyForm, DepartmentList, DeviceList, TerminalList)
- Department/device/terminal components are created in later tasks — use placeholder divs initially

- [ ] **Step 1: Create property detail page**

```tsx
// app/(finance)/finance/properties/[id]/page.tsx
import { notFound, redirect } from 'next/navigation'
import { createClient } from '@/lib/supabase/server'
import { requireRole } from '@/lib/finance/auth'
import { PropertyForm } from '@/components/finance/PropertyForm'
import { Tabs, TabsContent, TabsList, TabsTrigger } from '@/components/ui/tabs'
import type { Property, Department, FiscalDevice, POSTerminal } from '@/types/finance'

interface Props {
  params: Promise<{ id: string }>
}

export default async function PropertyDetailPage({ params }: Props) {
  const { id } = await params
  const user = await requireRole('ADMIN_CO')
  if (!user) redirect('/finance')

  const supabase = await createClient()

  const [
    { data: property },
    { data: departments },
    { data: devices },
    { data: terminals },
  ] = await Promise.all([
    supabase.from('properties').select('*').eq('id', id).single(),
    supabase.from('departments').select('*').eq('property_id', id).order('name'),
    supabase.from('fiscal_devices').select('*').eq('property_id', id).order('serial_number'),
    supabase.from('pos_terminals').select('*').eq('property_id', id).order('tid'),
  ])

  if (!property) notFound()

  return (
    <div className="p-6 max-w-4xl mx-auto">
      <h1 className="text-lg font-semibold mb-4">{property.name}</h1>

      <Tabs defaultValue="info">
        <TabsList>
          <TabsTrigger value="info">Информация</TabsTrigger>
          <TabsTrigger value="departments">
            Отдели ({departments?.length ?? 0})
          </TabsTrigger>
          <TabsTrigger value="devices">
            Фискални у-ва ({devices?.length ?? 0})
          </TabsTrigger>
          <TabsTrigger value="terminals">
            ПОС ({terminals?.length ?? 0})
          </TabsTrigger>
        </TabsList>

        <TabsContent value="info" className="mt-4">
          <PropertyForm property={property as Property} />
        </TabsContent>

        <TabsContent value="departments" className="mt-4">
          {/* DepartmentList — created in Task 11 */}
          <p className="text-sm text-muted-foreground">
            {(departments?.length ?? 0) === 0
              ? 'Няма добавени отдели.'
              : `${departments!.length} отдел(а)`}
          </p>
        </TabsContent>

        <TabsContent value="devices" className="mt-4">
          {/* DeviceList — created in Task 13 */}
          <p className="text-sm text-muted-foreground">
            {(devices?.length ?? 0) === 0
              ? 'Няма добавени фискални устройства.'
              : `${devices!.length} устройство(а)`}
          </p>
        </TabsContent>

        <TabsContent value="terminals" className="mt-4">
          {/* TerminalList — created in Task 13 */}
          <p className="text-sm text-muted-foreground">
            {(terminals?.length ?? 0) === 0
              ? 'Няма добавени ПОС терминали.'
              : `${terminals!.length} терминал(а)`}
          </p>
        </TabsContent>
      </Tabs>
    </div>
  )
}
```

- [ ] **Step 2: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 3: Commit**

```bash
git add app/(finance)/finance/properties/[id]/page.tsx
git commit -m "feat: add property detail page with tabs"
```

---

### Task 10: Departments API

API routes for listing departments (by property), creating, and updating them.

**Files:**
- Create: `app/api/finance/departments/route.ts`
- Create: `app/api/finance/departments/[id]/route.ts`

**Context:**
- `createDepartmentSchema` expects: property_id, name, manager_id, authorized_person_id (optional), fiscal_device_id (optional), pos_terminal_ids (optional — junction table, handled separately)
- `updateDepartmentSchema` is partial of the same (minus property_id)
- Only `ADMIN_CO` can create/update departments
- GET filters by `property_id` query parameter

- [ ] **Step 1: Create departments route.ts**

```typescript
// app/api/finance/departments/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'
import { createDepartmentSchema } from '@/lib/finance/schemas'

export async function GET(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }

  const propertyId = request.nextUrl.searchParams.get('property_id')
  if (!propertyId) {
    return NextResponse.json({ error: 'property_id required' }, { status: 400 })
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('departments')
    .select('*')
    .eq('property_id', propertyId)
    .order('name')

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data)
}

export async function POST(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const body = await request.json()
  const parsed = createDepartmentSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'validation_error', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  // Separate pos_terminal_ids (junction table) from the department row data
  const { pos_terminal_ids, ...departmentData } = parsed.data

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('departments')
    .insert(departmentData)
    .select()
    .single()

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  // Link POS terminals to department via junction table
  if (pos_terminal_ids && pos_terminal_ids.length > 0) {
    const junctionRows = pos_terminal_ids.map(tid => ({
      department_id: data.id,
      pos_terminal_id: tid,
    }))
    await supabase.from('department_pos_terminals').insert(junctionRows)
  }

  return NextResponse.json(data, { status: 201 })
}
```

- [ ] **Step 2: Create departments [id] route.ts**

```typescript
// app/api/finance/departments/[id]/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'
import { updateDepartmentSchema } from '@/lib/finance/schemas'

interface RouteParams {
  params: Promise<{ id: string }>
}

export async function PATCH(request: NextRequest, { params }: RouteParams) {
  const { id } = await params
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const body = await request.json()
  const parsed = updateDepartmentSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'validation_error', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('departments')
    .update(parsed.data)
    .eq('id', id)
    .select()
    .single()

  if (error || !data) {
    return NextResponse.json({ error: 'not_found' }, { status: 404 })
  }

  return NextResponse.json(data)
}
```

- [ ] **Step 3: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 4: Commit**

```bash
git add app/api/finance/departments/route.ts app/api/finance/departments/[id]/route.ts
git commit -m "feat: add departments API (GET list, POST create, PATCH update)"
```

---

### Task 11: Department list and form components

Client components for managing departments within a property detail page.

**Files:**
- Create: `components/finance/DepartmentList.tsx`
- Create: `components/finance/DepartmentForm.tsx`
- Modify: `app/(finance)/finance/properties/[id]/page.tsx` — replace placeholder with actual components

**Context:**
- DepartmentList shows table of departments with name, head (ID for now), is_active status
- "Добави отдел" button opens a dialog with DepartmentForm
- Edit button in each row opens DepartmentForm in edit mode
- Form POSTs to `/api/finance/departments` or PATCHes `/api/finance/departments/[id]`
- After success, `router.refresh()` to reload server data

- [ ] **Step 1: Create DepartmentForm.tsx**

```tsx
// components/finance/DepartmentForm.tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger,
} from '@/components/ui/dialog'
import type { Department } from '@/types/finance'

interface Props {
  propertyId: string
  department?: Department
  trigger: React.ReactNode
}

export function DepartmentForm({ propertyId, department, trigger }: Props) {
  const router = useRouter()
  const isEdit = !!department
  const [open, setOpen] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setError(null)
    setLoading(true)

    const formData = new FormData(e.currentTarget)
    const name = (formData.get('name') as string)?.trim()
    const managerId = (formData.get('manager_id') as string)?.trim() || null

    const body: Record<string, unknown> = isEdit
      ? { name, manager_id: managerId }
      : { property_id: propertyId, name, manager_id: managerId }

    try {
      const url = isEdit
        ? `/api/finance/departments/${department.id}`
        : '/api/finance/departments'
      const method = isEdit ? 'PATCH' : 'POST'

      const res = await fetch(url, {
        method,
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
      })

      if (!res.ok) {
        const data = await res.json()
        setError(data.message ?? data.error ?? 'Грешка при запис')
        return
      }

      setOpen(false)
      router.refresh()
    } catch {
      setError('Грешка при връзка със сървъра')
    } finally {
      setLoading(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>{trigger}</DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>{isEdit ? 'Редактиране на отдел' : 'Нов отдел'}</DialogTitle>
        </DialogHeader>
        <form onSubmit={handleSubmit} className="space-y-4">
          {error && (
            <p className="text-sm text-destructive bg-destructive/10 px-3 py-2 rounded">
              {error}
            </p>
          )}
          <div className="space-y-2">
            <Label htmlFor="dept-name">Име *</Label>
            <Input id="dept-name" name="name" required
              defaultValue={department?.name ?? ''} placeholder="напр. Рецепция" />
          </div>
          <div className="space-y-2">
            <Label htmlFor="dept-manager">Управител (ID) *</Label>
            <Input id="dept-manager" name="manager_id" required
              defaultValue={department?.manager_id ?? ''} placeholder="UUID на управител" />
          </div>
          <div className="flex gap-3 justify-end">
            <Button type="button" variant="outline" onClick={() => setOpen(false)}>
              Отказ
            </Button>
            <Button type="submit" disabled={loading}>
              {loading ? 'Запис...' : isEdit ? 'Запази' : 'Създай'}
            </Button>
          </div>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 2: Create DepartmentList.tsx**

```tsx
// components/finance/DepartmentList.tsx
'use client'

import { Badge } from '@/components/ui/badge'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import {
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import { DepartmentForm } from './DepartmentForm'
import { Plus, Pencil } from 'lucide-react'
import type { Department } from '@/types/finance'

interface Props {
  propertyId: string
  departments: Department[]
}

export function DepartmentList({ propertyId, departments }: Props) {
  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-4">
        <CardTitle className="text-base">Отдели</CardTitle>
        <DepartmentForm
          propertyId={propertyId}
          trigger={
            <Button size="sm">
              <Plus className="h-4 w-4 mr-2" />
              Добави отдел
            </Button>
          }
        />
      </CardHeader>
      <CardContent>
        {departments.length === 0 ? (
          <p className="text-sm text-muted-foreground py-4 text-center">
            Няма добавени отдели.
          </p>
        ) : (
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead>Име</TableHead>
                <TableHead>Управител (ID)</TableHead>
                <TableHead>Статус</TableHead>
                <TableHead className="w-10" />
              </TableRow>
            </TableHeader>
            <TableBody>
              {departments.map(dept => (
                <TableRow key={dept.id}>
                  <TableCell className="font-medium">{dept.name}</TableCell>
                  <TableCell className="text-muted-foreground font-mono text-xs">
                    {dept.manager_id}
                  </TableCell>
                  <TableCell>
                    <Badge variant={dept.status === 'ACTIVE' ? 'default' : 'secondary'}>
                      {dept.status === 'ACTIVE' ? 'Активен' : 'Неактивен'}
                    </Badge>
                  </TableCell>
                  <TableCell>
                    <DepartmentForm
                      propertyId={propertyId}
                      department={dept}
                      trigger={
                        <Button variant="ghost" size="icon" className="h-8 w-8">
                          <Pencil className="h-3.5 w-3.5" />
                        </Button>
                      }
                    />
                  </TableCell>
                </TableRow>
              ))}
            </TableBody>
          </Table>
        )}
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 3: Update property detail page to use DepartmentList**

In `app/(finance)/finance/properties/[id]/page.tsx`, add the import and replace the departments tab placeholder.

Add import at top:
```typescript
import { DepartmentList } from '@/components/finance/DepartmentList'
```

Replace the departments TabsContent:
```tsx
        <TabsContent value="departments" className="mt-4">
          <DepartmentList
            propertyId={id}
            departments={(departments as Department[]) ?? []}
          />
        </TabsContent>
```

- [ ] **Step 4: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 5: Commit**

```bash
git add components/finance/DepartmentList.tsx components/finance/DepartmentForm.tsx app/(finance)/finance/properties/[id]/page.tsx
git commit -m "feat: add department list and form within property detail"
```

---

### Task 12: Fiscal devices & POS terminals API

API routes for CRUD operations on fiscal devices and POS terminals.

**Files:**
- Create: `app/api/finance/fiscal-devices/route.ts`
- Create: `app/api/finance/fiscal-devices/[id]/route.ts`
- Create: `app/api/finance/pos-terminals/route.ts`
- Create: `app/api/finance/pos-terminals/[id]/route.ts`

**Context:**
- `createFiscalDeviceSchema` expects: property_id, serial_number, location (optional)
- `createPOSTerminalSchema` expects: property_id, terminal_id, bank, location (optional)
- Only `ADMIN_CO` can create/delete
- GET filters by `property_id` query param
- DELETE removes the device/terminal (no soft delete for these)

- [ ] **Step 1: Create fiscal-devices route.ts**

```typescript
// app/api/finance/fiscal-devices/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'
import { createFiscalDeviceSchema } from '@/lib/finance/schemas'

export async function GET(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }

  const propertyId = request.nextUrl.searchParams.get('property_id')
  if (!propertyId) {
    return NextResponse.json({ error: 'property_id required' }, { status: 400 })
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('fiscal_devices')
    .select('*')
    .eq('property_id', propertyId)
    .order('serial_number')

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data)
}

export async function POST(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const body = await request.json()
  const parsed = createFiscalDeviceSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'validation_error', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('fiscal_devices')
    .insert(parsed.data)
    .select()
    .single()

  if (error) {
    if (error.code === '23505') {
      return NextResponse.json(
        { error: 'duplicate', message: 'Устройство с този сериен номер вече съществува' },
        { status: 409 }
      )
    }
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data, { status: 201 })
}
```

- [ ] **Step 2: Create fiscal-devices [id] route.ts**

```typescript
// app/api/finance/fiscal-devices/[id]/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'

interface RouteParams {
  params: Promise<{ id: string }>
}

export async function DELETE(_request: NextRequest, { params }: RouteParams) {
  const { id } = await params
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const supabase = await createClient()
  const { error } = await supabase
    .from('fiscal_devices')
    .delete()
    .eq('id', id)

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return new NextResponse(null, { status: 204 })
}
```

- [ ] **Step 3: Create pos-terminals route.ts**

```typescript
// app/api/finance/pos-terminals/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'
import { createPOSTerminalSchema } from '@/lib/finance/schemas'

export async function GET(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user) {
    return NextResponse.json({ error: 'unauthorized' }, { status: 401 })
  }

  const propertyId = request.nextUrl.searchParams.get('property_id')
  if (!propertyId) {
    return NextResponse.json({ error: 'property_id required' }, { status: 400 })
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('pos_terminals')
    .select('*')
    .eq('property_id', propertyId)
    .order('tid')

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data)
}

export async function POST(request: NextRequest) {
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const body = await request.json()
  const parsed = createPOSTerminalSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { error: 'validation_error', details: parsed.error.flatten() },
      { status: 400 }
    )
  }

  const supabase = await createClient()
  const { data, error } = await supabase
    .from('pos_terminals')
    .insert(parsed.data)
    .select()
    .single()

  if (error) {
    if (error.code === '23505') {
      return NextResponse.json(
        { error: 'duplicate', message: 'Терминал с този TID вече съществува' },
        { status: 409 }
      )
    }
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return NextResponse.json(data, { status: 201 })
}
```

- [ ] **Step 4: Create pos-terminals [id] route.ts**

```typescript
// app/api/finance/pos-terminals/[id]/route.ts
import { NextRequest, NextResponse } from 'next/server'
import { createClient } from '@/lib/supabase/server'
import { getFinanceUser } from '@/lib/finance/auth'

interface RouteParams {
  params: Promise<{ id: string }>
}

export async function DELETE(_request: NextRequest, { params }: RouteParams) {
  const { id } = await params
  const user = await getFinanceUser()
  if (!user || user.role !== 'ADMIN_CO') {
    return NextResponse.json({ error: 'forbidden' }, { status: 403 })
  }

  const supabase = await createClient()
  const { error } = await supabase
    .from('pos_terminals')
    .delete()
    .eq('id', id)

  if (error) {
    return NextResponse.json({ error: 'database_error' }, { status: 500 })
  }

  return new NextResponse(null, { status: 204 })
}
```

- [ ] **Step 5: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 6: Commit**

```bash
git add app/api/finance/fiscal-devices/ app/api/finance/pos-terminals/
git commit -m "feat: add fiscal devices and POS terminals API routes"
```

---

### Task 13: Device and terminal list/form components

Client components for managing fiscal devices and POS terminals within property detail tabs.

**Files:**
- Create: `components/finance/DeviceList.tsx`
- Create: `components/finance/DeviceForm.tsx`
- Create: `components/finance/TerminalList.tsx`
- Create: `components/finance/TerminalForm.tsx`
- Modify: `app/(finance)/finance/properties/[id]/page.tsx` — wire up components

**Context:**
- Same pattern as DepartmentList/DepartmentForm: table + dialog for create
- Devices: serial_number, location, created_at — with delete button
- Terminals: terminal_id (TID), bank, location, created_at — with delete button
- Delete calls `DELETE /api/finance/fiscal-devices/[id]` or `pos-terminals/[id]`

- [ ] **Step 1: Create DeviceForm.tsx**

```tsx
// components/finance/DeviceForm.tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger,
} from '@/components/ui/dialog'

interface Props {
  propertyId: string
  trigger: React.ReactNode
}

export function DeviceForm({ propertyId, trigger }: Props) {
  const router = useRouter()
  const [open, setOpen] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setError(null)
    setLoading(true)

    const formData = new FormData(e.currentTarget)
    const body = {
      property_id: propertyId,
      serial_number: (formData.get('serial_number') as string)?.trim(),
      location: (formData.get('location') as string)?.trim() || null,
    }

    try {
      const res = await fetch('/api/finance/fiscal-devices', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
      })

      if (!res.ok) {
        const data = await res.json()
        setError(data.message ?? data.error ?? 'Грешка при запис')
        return
      }

      setOpen(false)
      router.refresh()
    } catch {
      setError('Грешка при връзка със сървъра')
    } finally {
      setLoading(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>{trigger}</DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Ново фискално устройство</DialogTitle>
        </DialogHeader>
        <form onSubmit={handleSubmit} className="space-y-4">
          {error && (
            <p className="text-sm text-destructive bg-destructive/10 px-3 py-2 rounded">
              {error}
            </p>
          )}
          <div className="space-y-2">
            <Label htmlFor="serial_number">Сериен номер *</Label>
            <Input id="serial_number" name="serial_number" required />
          </div>
          <div className="space-y-2">
            <Label htmlFor="device-location">Местоположение</Label>
            <Input id="device-location" name="location" placeholder="напр. Рецепция" />
          </div>
          <div className="flex gap-3 justify-end">
            <Button type="button" variant="outline" onClick={() => setOpen(false)}>
              Отказ
            </Button>
            <Button type="submit" disabled={loading}>
              {loading ? 'Запис...' : 'Добави'}
            </Button>
          </div>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 2: Create DeviceList.tsx**

```tsx
// components/finance/DeviceList.tsx
'use client'

import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import {
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import { DeviceForm } from './DeviceForm'
import { Plus, Trash2 } from 'lucide-react'
import type { FiscalDevice } from '@/types/finance'

interface Props {
  propertyId: string
  devices: FiscalDevice[]
}

export function DeviceList({ propertyId, devices }: Props) {
  const router = useRouter()

  async function handleDelete(id: string) {
    if (!confirm('Сигурни ли сте, че искате да изтриете това устройство?')) return

    await fetch(`/api/finance/fiscal-devices/${id}`, { method: 'DELETE' })
    router.refresh()
  }

  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-4">
        <CardTitle className="text-base">Фискални устройства</CardTitle>
        <DeviceForm
          propertyId={propertyId}
          trigger={
            <Button size="sm">
              <Plus className="h-4 w-4 mr-2" />
              Добави устройство
            </Button>
          }
        />
      </CardHeader>
      <CardContent>
        {devices.length === 0 ? (
          <p className="text-sm text-muted-foreground py-4 text-center">
            Няма добавени фискални устройства.
          </p>
        ) : (
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead>Сериен номер</TableHead>
                <TableHead>Местоположение</TableHead>
                <TableHead className="w-10" />
              </TableRow>
            </TableHeader>
            <TableBody>
              {devices.map(device => (
                <TableRow key={device.id}>
                  <TableCell className="font-mono text-sm">{device.serial_number}</TableCell>
                  <TableCell className="text-muted-foreground">
                    {device.location ?? '—'}
                  </TableCell>
                  <TableCell>
                    <Button
                      variant="ghost" size="icon" className="h-8 w-8 text-destructive"
                      onClick={() => handleDelete(device.id)}
                    >
                      <Trash2 className="h-3.5 w-3.5" />
                    </Button>
                  </TableCell>
                </TableRow>
              ))}
            </TableBody>
          </Table>
        )}
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 3: Create TerminalForm.tsx**

```tsx
// components/finance/TerminalForm.tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Input } from '@/components/ui/input'
import { Label } from '@/components/ui/label'
import {
  Dialog, DialogContent, DialogHeader, DialogTitle, DialogTrigger,
} from '@/components/ui/dialog'

interface Props {
  propertyId: string
  trigger: React.ReactNode
}

export function TerminalForm({ propertyId, trigger }: Props) {
  const router = useRouter()
  const [open, setOpen] = useState(false)
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: React.FormEvent<HTMLFormElement>) {
    e.preventDefault()
    setError(null)
    setLoading(true)

    const formData = new FormData(e.currentTarget)
    const body = {
      property_id: propertyId,
      tid: (formData.get('tid') as string)?.trim(),
      bank: (formData.get('bank') as string)?.trim(),
      location: (formData.get('location') as string)?.trim(),
    }

    try {
      const res = await fetch('/api/finance/pos-terminals', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body),
      })

      if (!res.ok) {
        const data = await res.json()
        setError(data.message ?? data.error ?? 'Грешка при запис')
        return
      }

      setOpen(false)
      router.refresh()
    } catch {
      setError('Грешка при връзка със сървъра')
    } finally {
      setLoading(false)
    }
  }

  return (
    <Dialog open={open} onOpenChange={setOpen}>
      <DialogTrigger asChild>{trigger}</DialogTrigger>
      <DialogContent>
        <DialogHeader>
          <DialogTitle>Нов ПОС терминал</DialogTitle>
        </DialogHeader>
        <form onSubmit={handleSubmit} className="space-y-4">
          {error && (
            <p className="text-sm text-destructive bg-destructive/10 px-3 py-2 rounded">
              {error}
            </p>
          )}
          <div className="space-y-2">
            <Label htmlFor="tid">TID *</Label>
            <Input id="tid" name="tid" required placeholder="Terminal ID" />
          </div>
          <div className="space-y-2">
            <Label htmlFor="terminal-bank">Банка *</Label>
            <Input id="terminal-bank" name="bank" required />
          </div>
          <div className="space-y-2">
            <Label htmlFor="terminal-location">Местоположение *</Label>
            <Input id="terminal-location" name="location" required placeholder="напр. Рецепция" />
          </div>
          <div className="flex gap-3 justify-end">
            <Button type="button" variant="outline" onClick={() => setOpen(false)}>
              Отказ
            </Button>
            <Button type="submit" disabled={loading}>
              {loading ? 'Запис...' : 'Добави'}
            </Button>
          </div>
        </form>
      </DialogContent>
    </Dialog>
  )
}
```

- [ ] **Step 4: Create TerminalList.tsx**

```tsx
// components/finance/TerminalList.tsx
'use client'

import { useRouter } from 'next/navigation'
import { Button } from '@/components/ui/button'
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card'
import {
  Table, TableBody, TableCell, TableHead, TableHeader, TableRow,
} from '@/components/ui/table'
import { TerminalForm } from './TerminalForm'
import { Plus, Trash2 } from 'lucide-react'
import type { POSTerminal } from '@/types/finance'

interface Props {
  propertyId: string
  terminals: POSTerminal[]
}

export function TerminalList({ propertyId, terminals }: Props) {
  const router = useRouter()

  async function handleDelete(id: string) {
    if (!confirm('Сигурни ли сте, че искате да изтриете този терминал?')) return

    await fetch(`/api/finance/pos-terminals/${id}`, { method: 'DELETE' })
    router.refresh()
  }

  return (
    <Card>
      <CardHeader className="flex flex-row items-center justify-between space-y-0 pb-4">
        <CardTitle className="text-base">ПОС терминали</CardTitle>
        <TerminalForm
          propertyId={propertyId}
          trigger={
            <Button size="sm">
              <Plus className="h-4 w-4 mr-2" />
              Добави терминал
            </Button>
          }
        />
      </CardHeader>
      <CardContent>
        {terminals.length === 0 ? (
          <p className="text-sm text-muted-foreground py-4 text-center">
            Няма добавени ПОС терминали.
          </p>
        ) : (
          <Table>
            <TableHeader>
              <TableRow>
                <TableHead>TID</TableHead>
                <TableHead>Банка</TableHead>
                <TableHead>Местоположение</TableHead>
                <TableHead className="w-10" />
              </TableRow>
            </TableHeader>
            <TableBody>
              {terminals.map(terminal => (
                <TableRow key={terminal.id}>
                  <TableCell className="font-mono text-sm">{terminal.tid}</TableCell>
                  <TableCell className="text-muted-foreground">{terminal.bank}</TableCell>
                  <TableCell className="text-muted-foreground">
                    {terminal.location ?? '—'}
                  </TableCell>
                  <TableCell>
                    <Button
                      variant="ghost" size="icon" className="h-8 w-8 text-destructive"
                      onClick={() => handleDelete(terminal.id)}
                    >
                      <Trash2 className="h-3.5 w-3.5" />
                    </Button>
                  </TableCell>
                </TableRow>
              ))}
            </TableBody>
          </Table>
        )}
      </CardContent>
    </Card>
  )
}
```

- [ ] **Step 5: Update property detail page to use all components**

In `app/(finance)/finance/properties/[id]/page.tsx`, add imports and replace remaining placeholders:

Add imports:
```typescript
import { DepartmentList } from '@/components/finance/DepartmentList'
import { DeviceList } from '@/components/finance/DeviceList'
import { TerminalList } from '@/components/finance/TerminalList'
```

Replace devices TabsContent:
```tsx
        <TabsContent value="devices" className="mt-4">
          <DeviceList
            propertyId={id}
            devices={(devices as FiscalDevice[]) ?? []}
          />
        </TabsContent>
```

Replace terminals TabsContent:
```tsx
        <TabsContent value="terminals" className="mt-4">
          <TerminalList
            propertyId={id}
            terminals={(terminals as POSTerminal[]) ?? []}
          />
        </TabsContent>
```

- [ ] **Step 6: Verify build**

```bash
npx tsc --noEmit
```

- [ ] **Step 7: Commit**

```bash
git add components/finance/DeviceForm.tsx components/finance/DeviceList.tsx components/finance/TerminalForm.tsx components/finance/TerminalList.tsx app/(finance)/finance/properties/[id]/page.tsx
git commit -m "feat: add fiscal device and POS terminal management in property detail"
```

---

### Task 14: Final verification

Run full build, lint, and verify all files exist.

**Files:**
- None (verification only)

- [ ] **Step 1: TypeScript check**

```bash
npx tsc --noEmit
```

Expected: no errors.

- [ ] **Step 2: Lint**

```bash
npm run lint
```

Expected: no errors.

- [ ] **Step 3: Verify file structure**

```bash
find app/(finance) -type f | sort
find app/api/finance -type f | sort
find components/finance -type f | sort
ls lib/finance/auth.ts
```

Expected output:
```
app/(finance)/finance/page.tsx
app/(finance)/finance/properties/[id]/page.tsx
app/(finance)/finance/properties/new/page.tsx
app/(finance)/finance/properties/page.tsx
app/(finance)/layout.tsx
app/api/finance/departments/[id]/route.ts
app/api/finance/departments/route.ts
app/api/finance/fiscal-devices/[id]/route.ts
app/api/finance/fiscal-devices/route.ts
app/api/finance/pos-terminals/[id]/route.ts
app/api/finance/pos-terminals/route.ts
app/api/finance/properties/[id]/route.ts
app/api/finance/properties/route.ts
components/finance/DepartmentForm.tsx
components/finance/DepartmentList.tsx
components/finance/DeviceForm.tsx
components/finance/DeviceList.tsx
components/finance/FinanceSidebar.tsx
components/finance/PropertyForm.tsx
components/finance/PropertyList.tsx
components/finance/TerminalForm.tsx
components/finance/TerminalList.tsx
lib/finance/auth.ts
```

- [ ] **Step 4: Verify build compiles**

```bash
npm run build 2>&1 | tail -20
```

Expected: build succeeds.
